import { Timestamp } from 'rxjs';

export interface AssetsOther {
  id: number;
  name: string;
  description: string;
  assetID: number;
  created_at: string;
  updated_at: string;
  status: string;
}
